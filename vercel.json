{
  "version": 2,
  "builds": [
    {
      "src": "api/index.py",  // Точный путь к файлу функции
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",        // Все запросы
      "dest": "api/index.py" // Направляются в файл функции
    }
  ]
}